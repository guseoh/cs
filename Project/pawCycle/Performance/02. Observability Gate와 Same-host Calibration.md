---
category: PawCycle
status: 완료
tags:
  - Project
  - pawCycle
  - Performance
  - Observability
  - OCI
---

# Observability Gate와 Same-host Calibration

일반 개념은 [[../../../Study/Performance/03. Observability와 계층별 병목 분리|Observability와 계층별 병목 분리]]와 [[../../../Study/Performance/08. Observer Effect와 Same-host Calibration|Observer Effect와 Same-host Calibration]]에서 다룬다.

이 문서는 PawCycle OCI Production에서 실제로 수행한 Gate와 측정 결과만 기록한다.

## 목표

PERF-V7-001에서 OCI Re-baseline 전에 다음 조건을 검증했다.

~~~text
Production READY
Observability NORMAL
Prometheus target up
Application identity 불변
latest approved SHA provenance
same-host overhead 허용 가능
~~~

Canonical evidence:
[Issue #300](https://github.com/guseoh/pawcycle-commerce/issues/300)

## 환경

- OCI app01
- CPU 2
- Application: Backend / Frontend / Nginx
- Observability: Prometheus / Grafana
- metrics bridge: metrics-proxy
- Database: OCI Managed MySQL
- approved main: df74023e96a3afe37219ecd50d48adac8b7b3c60

OFF/ON에서 변경하지 않은 것:

- Backend
- Frontend
- Nginx
- Managed MySQL
- HTTPS/certificate
- release SHA
- Application network
- named volume

## 시작 상태

Production:

~~~text
production_assessment=READY
release_coordination=stable
docker_query=ok
backend=healthy
api_products_http=200
metrics_proxy_http=200
~~~

Observability:

~~~text
status=NORMAL
production_assessment=READY
prometheus_target=up
~~~

Initial ON runtime source는 historical approved SHA였지만 관련 configuration checksum drift는 없었다.

최종 Restored ON은 exact latest approved SHA source로 다시 올려 provenance를 정렬했다.

## 실행 순서

~~~text
1. Initial ON 60초
2. Prometheus/Grafana OFF
3. metrics-proxy OFF
4. Application identity 확인
5. OFF 60초
6. latest-SHA metrics-proxy 복구
7. latest-SHA Prometheus/Grafana 복구
8. READY / NORMAL / target up 확인
9. runtime provenance 확인
10. Restored ON 60초
~~~

각 window:

- GET /api/products
- 30 samples
- 2초 간격
- vmstat 1 60
- free -b
- df -P /
- docker stats --no-stream
- restart/OOM 확인

## 측정 결과

| Metric | Initial ON | OFF | Restored ON |
| --- | ---: | ---: | ---: |
| 2xx | 30/30 | 30/30 | 30/30 |
| Error | 0 | 0 | 0 |
| p50 | 23.164 ms | 23.016 ms | 23.442 ms |
| p95 | 24.989 ms | 31.810 ms | 32.977 ms |
| p99 | 277.231 ms | 150.187 ms | 57.793 ms |
| Max | 379.872 ms | 198.229 ms | 67.281 ms |
| CPU user avg | 약 1.76% | 1.356% | 1.763% |
| CPU system avg | 약 1.42% | 1.119% | 1.441% |
| CPU idle avg | 약 97.08% | 97.746% | 97.102% |
| I/O wait | 0% | 0% | 0% |

최종 비교는 OFF와 Restored ON이다.

~~~text
p50
23.016 → 23.442 ms
+0.426 ms

p95
31.810 → 32.977 ms
+1.167 ms

CPU user + system
2.475% → 3.204%
약 +0.73%p

HTTP error
0 → 0

I/O wait
0 → 0
~~~

## Memory

OFF MemAvailable:

~~~text
start 4,129,521,664
end   4,144,447,488 bytes
~~~

Restored ON:

~~~text
start 3,891,687,424
end   3,872,800,768 bytes
~~~

Observability ON에서 약 227~259 MiB 적었다.

Restored ON snapshot:

- Grafana 약 216.9 MiB / 256 MiB
- Prometheus 약 49.62 MiB / 384 MiB
- metrics-proxy 약 4.7~4.9 MiB / 32 MiB

측정 중:

~~~text
restart = 0
OOM = 0
swap in/out avg = 0 / 0
Application identity = unchanged
~~~

## Provenance

복구 후 metrics-proxy, Prometheus, Grafana Compose working directory가 승인 SHA exact source 하위 경로인지 확인했다.

~~~text
latest_sha_runtime_provenance=PASS
~~~

Application Backend/Frontend/Nginx container identity도 OFF/ON 전후 동일했다.

## 판정

~~~text
same-host Observability = KEEP
separate Observability Compute = DEFER
same-host overhead calibration = PASS
runtime 측면 Performance Measurement Ready = PASS
~~~

근거:

- HTTP 90/90 성공
- OFF 대비 Restored ON p50 +0.426ms
- p95 +1.167ms
- CPU user+system 약 +0.73%p
- I/O wait 증가 없음
- OOM/restart 없음
- Application identity 불변
- latest SHA provenance 정렬

Memory 비용은 실제로 존재하지만 이번 calibration에서 baseline을 사용할 수 없을 정도의 latency/error/CPU 왜곡은 확인되지 않았다.

## 남은 범위

PERF-V7-001 전체는 아직 IN PROGRESS다.

- deploy.lock repository recurrence prevention
- 최소 performance metric set 확정
- Scale Dataset / OCI Re-baseline handoff

Troubleshooting은 [[03. Performance Gate Troubleshooting]]에서 이어진다.
